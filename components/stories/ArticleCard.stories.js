import { ArticleCard } from '../../pages/index';

export default {
  title: 'Style Guide/Components/Card/ArticleCard',
  component: ArticleCard
};

const Template = (args) => <ArticleCard {...args} />;

export const Default = Template.bind({});
